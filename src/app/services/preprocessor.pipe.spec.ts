import { PreprocessorPipe } from './preprocessor.pipe';

describe('PipePipe', () => {
  it('create an instance', () => {
    const pipe = new PreprocessorPipe();
    expect(pipe).toBeTruthy();
  });
});
